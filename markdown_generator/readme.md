# Publication markdown generators

These optional scripts and notebooks convert publication data from TSV, BibTeX,
or ORCID into Jekyll markdown files. The live site does not require them to
build; they are retained as maintenance tools for updating the publication
collection.

